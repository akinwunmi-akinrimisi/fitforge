/**
 * Placeholder for generated Supabase types.
 *
 * Regenerate after each migration via:
 *   pnpm gen:types
 *
 * Until the types are generated, the typed client falls back to `any` for
 * row shapes — tolerable during M1 bootstrap. Generate before merging M2.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
